var selector = "#answer_" + <%= @answer.id %> + " > .grid_16";
$(selector).empty();
$(selector).hide();
$(selector).append("<%= escape_javascript(raw @answer.body) %>");  
$(selector).fadeIn(1000);

if (CKEDITOR.instances['answer_edit'] != null){
delete CKEDITOR.instances['answer_edit'];
}

var wrapper = "#reply_edit_wrapper_" + <%= @answer.id %>;
$(wrapper).show();

<% if @badge %>
  $.notifyBar({
    html: "<%= 'Vous avez gagné le badge ' + @badge  %>",
    delay: 2000,
    animationSpeed: "normal"
  });  
<% end %>
